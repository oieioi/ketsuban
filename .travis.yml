language: ruby

cache: bundler

services:
  - postgresql
  - mysql

rvm:
  - 2.5

before_script:
  - cd test/dummy
  - bin/rake db:create
  - bin/rake db_mysql:create

script:
  - cd ../../
  - bin/test
